# VoxProof Cursor Adaptation

Adapted from `.reference/vox-proof` governance (SHA `17f62b6…`).

## Patterns kept

- `AGENTS.md` entry with required reading order
- Always-on work-package and model-budget rules
- Bounded change skills and acceptance packet discipline
- Read-only reference source rule
- Single auditor pass with correction limit

## Patterns simplified

- No persistence evidence worker swarm or cross-platform SQLite spike workflow
- No GPT-5.6 Sol final conflict reviewer (explicitly forbidden for foundation)
- No storage-systems reviewer agents
- Archaeology + audit use Cursor-native Grok preference; implementation uses Composer 2.5 Fast

## Model accounting

Every report records requested/actual models and `api_quota_used: false` unless owner authorizes API models.

Review date for model-budget rule: **2026-08-11**.
