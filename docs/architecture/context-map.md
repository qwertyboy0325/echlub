# Context Map

```text
[Browser Lab UI] --WASM--> [echlub-web] --> [kernel/model]
[protocol-lab] -------------------------> [replication/kernel]
[control-plane] ------------------------> [session/protocol/model]
[Future transport adapters] ------------> [protocol]
[Future performance engine] ------------> (not selected)
[Legacy references .reference/] ------> read-only archaeology
```

Legacy repositories are not runtime dependencies.
